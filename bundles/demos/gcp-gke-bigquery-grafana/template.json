{
  "name": "gcp-gke-bigquery-grafana-finops",
  "description": "FinOps bundle for GCP + GKE + BigQuery stacks - billing analysis, GKE rightsizing, cost forecasting via BigQuery billing exports",
  "mcpServers": {
    "aws-cost-explorer": {
      "command": "stn",
      "args": ["mock", "aws-cost-explorer"]
    },
    "grafana": {
      "command": "stn",
      "args": ["mock", "grafana"]
    }
  }
}
