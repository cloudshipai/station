{
  "name": "faker-test-env",
  "description": "Test environment for faker with tool caching",
  "mcpServers": {
    "aws-cloudwatch-faker": {
      "command": "stn",
      "args": [
        "faker",
        "--command", "npx",
        "--args", "-y,@modelcontextprotocol/server-filesystem@latest,/tmp",
        "--ai-instruction", "Simulate AWS CloudWatch metrics showing steady 50% CPU usage, healthy service status, and no active alarms. Generate realistic metric data with proper timestamps and AWS-specific fields.",
        "--debug"
      ]
    }
  }
}
