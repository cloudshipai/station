version: 2.1

jobs:
  analyze:
    docker:
      - image: ghcr.io/cloudshipai/station:latest
    steps:
      - checkout
      - run:
          name: Run Agent
          command: stn agent run "Code Reviewer" "Review the code for bugs and best practices"

workflows:
  main:
    jobs:
      - analyze:
          context: station
