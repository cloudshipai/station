{
  "name": "GitHub Integration",
  "description": "Official GitHub MCP server for repository management and operations",
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@github/github-mcp-server"],
      "env": {
        "GITHUB_TOKEN": "{{GITHUB_TOKEN}}"
      }
    }
  },
  "templates": {
    "GITHUB_TOKEN": {
      "description": "GitHub Personal Access Token with repo permissions",
      "type": "password",
      "required": true,
      "sensitive": true,
      "help": "Create at: https://github.com/settings/tokens/new"
    }
  }
}