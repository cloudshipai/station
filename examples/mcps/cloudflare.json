{
  "name": "Cloudflare DNS & CDN Management",
  "description": "Manage Cloudflare DNS records, workers, and CDN settings",
  "mcpServers": {
    "cloudflare": {
      "command": "npx",
      "args": ["-y", "@mcp/server-cloudflare"],
      "env": {
        "CLOUDFLARE_API_TOKEN": "{{CLOUDFLARE_API_TOKEN}}",
        "CLOUDFLARE_ZONE_ID": "{{CLOUDFLARE_ZONE_ID}}",
        "CLOUDFLARE_ACCOUNT_ID": "{{CLOUDFLARE_ACCOUNT_ID}}"
      }
    }
  },
  "templates": {
    "CLOUDFLARE_API_TOKEN": {
      "description": "Cloudflare API token",
      "type": "password",
      "required": true,
      "sensitive": true,
      "help": "Create at: https://dash.cloudflare.com/profile/api-tokens"
    },
    "CLOUDFLARE_ZONE_ID": {
      "description": "Cloudflare Zone ID for your domain",
      "type": "string",
      "required": false,
      "help": "Found in your domain's Overview tab in Cloudflare dashboard"
    },
    "CLOUDFLARE_ACCOUNT_ID": {
      "description": "Cloudflare Account ID",
      "type": "string",
      "required": false,
      "help": "Found in the right sidebar of Cloudflare dashboard"
    }
  }
}