{
  "name": "Semgrep Security Scanner",
  "description": "Allows AI agents to scan code for security vulnerabilities using Semgrep",
  "mcpServers": {
    "semgrep": {
      "command": "npx",
      "args": ["-y", "@semgrep/mcp-server"],
      "env": {
        "SEMGREP_APP_TOKEN": "{{SEMGREP_APP_TOKEN}}"
      }
    }
  },
  "templates": {
    "SEMGREP_APP_TOKEN": {
      "description": "Semgrep App Token for API access",
      "type": "password",
      "required": false,
      "sensitive": true,
      "help": "Optional: Get from https://semgrep.dev/manage/settings/tokens"
    }
  }
}