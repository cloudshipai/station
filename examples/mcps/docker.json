{
  "mcpServers": {
    "docker": {
      "command": "npx",
      "args": ["-y", "@mcp/server-docker"],
      "env": {
        "DOCKER_HOST": "{{.DOCKER_HOST}}",
        "DOCKER_COMPOSE_PATH": "{{.DOCKER_COMPOSE_PATH}}"
      }
    }
  }
}