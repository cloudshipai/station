{
  "name": "filesystem",
  "servers": {
    "filesystem-server": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "{{.AllowedPaths}}"],
      "env": {}
    }
  }
}