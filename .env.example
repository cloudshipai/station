# Station Configuration Example
# Copy this file to .env and update the values

# Required: 32-byte encryption key for MCP config and sensitive data encryption
# Generate with: openssl rand -hex 32
# Must be exactly 64 hexadecimal characters (32 bytes)
ENCRYPTION_KEY=your-32-byte-key-here-must-be-exactly-64-hex-chars

# Database configuration
DATABASE_URL=station.db

# Server ports
SSH_PORT=2222
MCP_PORT=3000  
API_PORT=8080

# SSH configuration
SSH_HOST_KEY_PATH=./ssh_host_key
ADMIN_USERNAME=admin

# Optional: Debug mode (set to true for verbose logging)
# STATION_DEBUG=false

# Optional: Model provider configurations
# These can also be configured through the admin interface
# OPENAI_API_KEY=your-openai-api-key-here
# ANTHROPIC_API_KEY=your-anthropic-api-key-here