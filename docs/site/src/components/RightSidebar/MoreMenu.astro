---
import { COMMUNITY_INVITE_URL } from '../../consts'

type Props = {
  editHref: string
}

const { editHref } = Astro.props
const showMoreSection = Boolean(COMMUNITY_INVITE_URL)
---

<!-- Community link moved to header/footer -->

